#include <vector>
using namespace std;

int firstDuplicateValue(vector<int> array) { 

	map<int,int>val;
	
	for(int i=0;i<array.size();i++){
		if(val.count(array[i])>0){
			return array[i];
		}
		else{
			val[array[i]]=1;
		}
	}
	return -1;
	
	
	
	
	return -1; 
}
